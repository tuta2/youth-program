<%= form_with(model: southrift) do |form| %>
  <% if southrift.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(southrift.errors.count, "error") %> prohibited this southrift from being saved:</h2>

      <ul>
        <% southrift.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
