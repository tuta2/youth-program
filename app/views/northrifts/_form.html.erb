<%= form_with(model: northrift) do |form| %>
  <% if northrift.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(northrift.errors.count, "error") %> prohibited this northrift from being saved:</h2>

      <ul>
        <% northrift.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
