<%= form_with(model: patrolleaderscourse) do |form| %>
  <% if patrolleaderscourse.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(patrolleaderscourse.errors.count, "error") %> prohibited this patrolleaderscourse from being saved:</h2>

      <ul>
        <% patrolleaderscourse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
