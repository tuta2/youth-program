<%= form_with(model: western) do |form| %>
  <% if western.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(western.errors.count, "error") %> prohibited this western from being saved:</h2>

      <ul>
        <% western.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
