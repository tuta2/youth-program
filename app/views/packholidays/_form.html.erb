<%= form_with(model: packholiday) do |form| %>
  <% if packholiday.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(packholiday.errors.count, "error") %> prohibited this packholiday from being saved:</h2>

      <ul>
        <% packholiday.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
